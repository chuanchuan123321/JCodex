# Skill Store

Place catalog skill folders in this directory. Each folder must contain a
`SKILL.md` file. Installing a catalog entry copies it to `workspace/skills`;
removing an installed skill never modifies this catalog copy.

Set `SKILL_STORE_PATH` to use a different local catalog directory.
